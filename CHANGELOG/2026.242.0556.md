# 2026.242.0556 — fix: add a root charly.yml so the candy CI gate is real

## Summary

Makes this repo's candy CI gate actually gate something.

Without a root `charly.yml` a plugin repo is **project-less**: charly scans **no candies at
all**, so the stock `charly box validate` step in `deploy.yml` exits 0 on any manifest —
including one with a malformed `plugin.providers` entry. The step has been asserting nothing
for this repo.

Two commits:

1. **Add the root manifest** — a four-line `discover:` block. The candy becomes scannable,
   the CUE schema gate applies, and the **stock** step starts working with no workflow change.
2. **Correct its schema stamp** — my first commit stamped `2026.240.1943` (current HEAD),
   but this repo's `deploy.yml` pins charly `v2026.238.1242`, whose maximum supported schema
   is `2026.232.0520`. That mismatch failed the `candy` workflow; the second commit stamps
   `2026.232.0520`, the value the peer candy repos already carry.

The file declares no entities of its own; it only tells charly where to look. Nothing under
`candy/` is touched — the schema requirement applies to the project **root** manifest only.

Refs opencharly/charly#459.

## How tested

Run against a binary built from **this repo's own pinned charly tag** (`v2026.238.1242`), not
a newer local build — using the wrong toolchain is exactly what caused the stamp mismatch
above.

**The manifest is accepted at the corrected stamp** (this repo, this branch):

```
$ charly box validate
(no output — exit 0)
```

**And the gate now bites.** Changing this repo's own `kind:resource` provider entry in
`candy/plugin-resource/charly.yml` to `notaclass:` — which previously passed at the repo root:

```
charly: error: command "validate": 2 validation errors:

candy "plugin-resource": CUE schema: /home/atrawog/Sync/Atrapub/coder/claude/work/prwork/plugin-resource/candy/plugin-resource/charly.yml: candy "plugin-resource": 2 errors in empty disjunction:
```

That failure is the point of the change: before the root manifest, that same malformed
manifest exited **0** here.

## Rulebook compliance

- **R0 (skills)** — `/charly-internals:plugin`, `/charly-internals:git-workflow`.
- **R1 (RCA)** — root-caused rather than guessed: an earlier hypothesis (that the legacy
  `candy/` walk skipped the CUE gate) was disproved — charly scans nothing at all without a
  root manifest. The stamp regression was likewise root-caused to the CI charly pin, by
  building that tag and testing three stamps against it.
- **R2 (no split)** — nothing deferred: this repo is fixed here, and the same defect is being
  closed across every affected plugin repo in the same sweep (opencharly/charly#459).
- **R3 (no duplication)** — one gate per repo; the stock step is made to work rather than a
  parallel validation being added.
- **R4 (no workarounds)** — the supported mechanism (`discover:`); the failing gate was fixed,
  never suppressed or made non-blocking.
- **R4a (fix the product first)** — the core option (make `validate` treat a `candy/`-bearing
  dir as a project) was investigated and rejected: "returns nil for a project-less directory"
  is a documented contract other resolve paths rely on.
- **R5 (hard cutover)** — the wrong stamp is replaced outright; no dead value remains.
- **R6 (git safety)** — feature branch, no force-push, no hook bypass.
- **R7 (prove the gate)** — this repo's own before/after output is above, from its own pinned
  toolchain.
- **R9 (deployed binary)** — N/A — no binary or runtime path; the provider is untouched.
- **R10 (fresh disposable proof)** — N/A — the change cannot reach a running deployment; the
  validate pair fully observes it.
- **RDD** — the assumption that broke the first commit ("HEAD stamp is fine everywhere") is
  now measured against the pinned tag rather than assumed.
- **ADE / SDD** — N/A — no install content authored, no schema or generated code touched.
- **Concurrency mandate** — N/A — a four-line file.
- **Architecture gate / placement** — correct: the repo declares its own shape at a version
  its own CI can read; core keeps its project-less contract unchanged.

## Change classification

- **Change class:** repo configuration — adds a root manifest so the existing CI gate has
  input, at the schema its CI charly supports.
- **Verification gate:** `charly box validate` in **this repo**, pristine and with a
  deliberately malformed provider entry, under this repo's pinned charly.
- **Attribution tier:** `fully tested and validated` — the gate this change exists to repair
  was exercised in both directions in this repo, with its output retained above.

*Assisted-by: Claude Code claude-opus-5[1m] (fully tested and validated)*